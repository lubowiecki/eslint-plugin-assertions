import { assertionCode } from './rules/assertion-code';
import { assertionCondition } from './rules/assertion-condition';

export = {
	rules: {
		'assertions-code': assertionCode,
		'assertions-condition': assertionCondition,
	},
};
